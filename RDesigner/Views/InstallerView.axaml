<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:Pyramid.ViewModels"
             mc:Ignorable="d"
             d:DesignWidth="1130"
             d:DesignHeight="620"
             x:Class="Pyramid.Views.InstallerView"
             x:DataType="viewModels:InstallerViewModel">
    <Grid Margin="28">
        <Grid IsVisible="{Binding IsWelcomeVisible}"
              RowDefinitions="Auto,*,Auto">
            <StackPanel Spacing="10">
                <TextBlock Text="Pyramid"
                           FontSize="34"
                           FontWeight="SemiBold" />
                <TextBlock Text="{DynamicResource InstallerWelcomeText}"
                           FontSize="18"
                           Foreground="#424242" />
            </StackPanel>

            <Grid Grid.Row="1"
                  Width="820"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  ColumnDefinitions="*,*"
                  RowDefinitions="Auto,Auto">
                <Button Command="{Binding OpenServerInstallCommand}"
                        MinHeight="128"
                        Margin="0 0 18 0"
                        Padding="20"
                        Background="#0B63CE"
                        Foreground="White"
                        HorizontalContentAlignment="Stretch">
                    <StackPanel Spacing="8">
                        <TextBlock Text="{DynamicResource InstallerServerInstallTitle}"
                                   FontSize="20"
                                   FontWeight="SemiBold" />
                        <TextBlock Text="{DynamicResource InstallerServerInstallDescription}"
                                   TextWrapping="Wrap"
                                   FontSize="14"
                                   Opacity="0.92" />
                    </StackPanel>
                </Button>

                <Button Grid.Column="1"
                        MinHeight="128"
                        Margin="18 0 0 0"
                        Padding="20"
                        Background="#0B63CE"
                        Foreground="White"
                        IsEnabled="False"
                        HorizontalContentAlignment="Stretch">
                    <StackPanel Spacing="8">
                        <TextBlock Text="{DynamicResource InstallerClientInstallTitle}"
                                   FontSize="20"
                                   FontWeight="SemiBold" />
                        <TextBlock Text="{DynamicResource InstallerClientInstallDescription}"
                                   TextWrapping="Wrap"
                                   FontSize="14"
                                   Opacity="0.92" />
                    </StackPanel>
                </Button>

                <Button Grid.Row="1"
                        Grid.ColumnSpan="2"
                        Width="390"
                        MinHeight="116"
                        Margin="0 34 0 0"
                        Padding="20"
                        Background="#0B63CE"
                        Foreground="White"
                        IsEnabled="False"
                        HorizontalAlignment="Center"
                        HorizontalContentAlignment="Stretch">
                    <StackPanel Spacing="8">
                        <TextBlock Text="{DynamicResource InstallerAdvancedTitle}"
                                   FontSize="20"
                                   FontWeight="SemiBold" />
                        <TextBlock Text="{DynamicResource InstallerAdvancedDescription}"
                                   TextWrapping="Wrap"
                                   FontSize="14"
                                   Opacity="0.92" />
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>

        <Grid IsVisible="{Binding IsLicenseVisible}"
              Width="880"
              HorizontalAlignment="Center"
              RowDefinitions="Auto,*,Auto,Auto">
            <TextBlock Text="{DynamicResource InstallerLicenseTitle}"
                       HorizontalAlignment="Center"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Margin="0 0 0 20" />

            <Border Grid.Row="1"
                    BorderBrush="#B8C5D6"
                    BorderThickness="1"
                    Background="#FAFAFA"
                    Padding="18">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="LicenseDocumentPanel" />
                </ScrollViewer>
            </Border>

            <CheckBox Grid.Row="2"
                      Content="{DynamicResource InstallerLicenseAcceptText}"
                      IsChecked="{Binding IsLicenseAccepted}"
                      Margin="0 18 0 18"
                      FontSize="15" />

            <Grid Grid.Row="3"
                  ColumnDefinitions="Auto,*,Auto">
                <Button Content="{DynamicResource InstallerBackButton}"
                        Command="{Binding BackToWelcomeCommand}"
                        MinWidth="110"
                        Height="38" />
                <Button Grid.Column="2"
                        Content="{DynamicResource InstallerNextButton}"
                        Command="{Binding ContinueToServerInstallCommand}"
                        MinWidth="110"
                        Height="38"
                        Background="#0B63CE"
                        Foreground="White" />
            </Grid>
        </Grid>

        <Grid IsVisible="{Binding IsServerInstallVisible}"
              RowDefinitions="Auto,Auto,Auto,*">
            <Grid ColumnDefinitions="Auto,Auto,*">
                <Button Command="{Binding BackToLicenseCommand}"
                        Width="36"
                        Height="32"
                        Padding="0"
                        Margin="0 1 12 0"
                        Background="Transparent"
                        BorderBrush="#B8C5D6"
                        Foreground="#0B63CE"
                        ToolTip.Tip="{DynamicResource InstallerBackButton}">
                    <TextBlock Text="←"
                               FontSize="22"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Button>
                <TextBlock Grid.Column="1"
                           Text="{DynamicResource InstallerSubtitle}"
                           FontSize="28"
                           FontWeight="SemiBold" />
            </Grid>

            <Grid Grid.Row="1"
                  ColumnDefinitions="Auto,*,Auto"
                  Margin="0 18 0 8"
                  IsVisible="{Binding IsWindows}">
                <TextBlock Text="{DynamicResource InstallerInstallFolder}"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold" />
                <TextBox Grid.Column="1"
                         Text="{Binding WindowsInstallDirectory}"
                         Watermark="{DynamicResource InstallerInstallFolderWatermark}"
                         Height="34"
                         Margin="12 0 0 0" />
                <Button Grid.Column="2"
                        Content="{DynamicResource InstallerBrowse}"
                        Command="{Binding BrowseInstallDirectoryCommand}"
                        MinWidth="96"
                        Height="34"
                        Margin="12 0 0 0" />
            </Grid>

            <TextBlock Grid.Row="1"
                       IsVisible="{Binding IsLinux}"
                       Text="{DynamicResource InstallerLinuxPostgresSystemInstallNote}"
                       Foreground="#555"
                       FontSize="14"
                       Margin="0 18 0 8"
                       TextWrapping="Wrap" />

            <Grid Grid.Row="2"
                  ColumnDefinitions="Auto,Auto,Auto,*"
                  Margin="0 8 0 18">
                <Button Content="{DynamicResource InstallerSilentInstall}"
                        Command="{Binding InstallArmCommand}"
                        MinWidth="170"
                        Height="40"
                        Background="#0B63CE"
                        Foreground="White" />
                <ProgressBar Grid.Column="1"
                             IsVisible="{Binding IsArmInstallProgressVisible}"
                             IsIndeterminate="True"
                             Width="38"
                             Height="8"
                             Margin="12 0 0 0"
                             VerticalAlignment="Center"
                             Foreground="#1FA34A"
                             Background="#D9F2E2" />
                <TextBlock Grid.Column="1"
                           IsVisible="{Binding IsArmInstallCompletedVisible}"
                           Text="✓"
                           Margin="12 0 0 0"
                           VerticalAlignment="Center"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="#1FA34A" />
                <TextBlock Grid.Column="2"
                           IsVisible="{Binding IsArmInstallCancelledVisible}"
                           Text="{DynamicResource InstallerArmInstallCancelledShort}"
                           Margin="12 0 0 0"
                           VerticalAlignment="Center"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#C62828" />
                <TextBlock Grid.Column="2"
                           IsVisible="{Binding IsArmInstallSystemCancelledVisible}"
                           Text="{DynamicResource InstallerArmInstallSystemCancelledShort}"
                           Margin="12 0 0 0"
                           VerticalAlignment="Center"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#C62828" />
            </Grid>

            <Border Grid.Row="3"
                    BorderBrush="#D0D0D0"
                    BorderThickness="1"
                    Background="#FAFAFA"
                    Padding="12">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding LogText}"
                               FontFamily="Consolas"
                               FontSize="13"
                               TextWrapping="NoWrap">
                        <TextBlock.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="{DynamicResource InstallerCopy}"
                                          Command="{Binding CopyLogCommand}" />
                            </ContextMenu>
                        </TextBlock.ContextMenu>
                    </TextBlock>
                </ScrollViewer>
            </Border>
        </Grid>

        <ComboBox x:Name="LanguageSelector"
                  SelectionChanged="OnLanguageSelectionChanged"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Top"
                  Width="142"
                  Height="34"
                  Padding="8 0"
                  Cursor="Hand"
                  IsEnabled="{Binding IsLanguageSelectionEnabled}"
                  ZIndex="10">
            <ComboBoxItem Tag="Russian">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Border Width="24"
                            Height="16"
                            BorderBrush="#A0A0A0"
                            BorderThickness="0.5"
                            VerticalAlignment="Center">
                        <Grid RowDefinitions="*,*,*">
                            <Border Background="White" />
                            <Border Grid.Row="1" Background="#1C57A7" />
                            <Border Grid.Row="2" Background="#D52B1E" />
                        </Grid>
                    </Border>
                    <TextBlock Text="русский"
                               VerticalAlignment="Center" />
                </StackPanel>
            </ComboBoxItem>
            <ComboBoxItem Tag="English">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Border Width="24"
                            Height="16"
                            BorderBrush="#A0A0A0"
                            BorderThickness="0.5"
                            VerticalAlignment="Center"
                            ClipToBounds="True">
                        <Viewbox Stretch="Fill">
                            <Canvas Width="30" Height="20" Background="#012169">
                                <Path Fill="White" Data="M 0,0 L 4,0 L 30,17 L 30,20 L 26,20 L 0,3 Z" />
                                <Path Fill="White" Data="M 26,0 L 30,0 L 30,3 L 4,20 L 0,20 L 0,17 Z" />
                                <Path Fill="#C8102E" Data="M 0,0 L 2,0 L 30,18 L 30,20 L 28,20 L 0,2 Z" />
                                <Path Fill="#C8102E" Data="M 28,0 L 30,0 L 30,2 L 2,20 L 0,20 L 0,18 Z" />
                                <Rectangle Width="30" Height="6" Fill="White" Canvas.Top="7" />
                                <Rectangle Width="8" Height="20" Fill="White" Canvas.Left="11" />
                                <Rectangle Width="30" Height="3" Fill="#C8102E" Canvas.Top="8.5" />
                                <Rectangle Width="4" Height="20" Fill="#C8102E" Canvas.Left="13" />
                            </Canvas>
                        </Viewbox>
                    </Border>
                    <TextBlock Text="english"
                               VerticalAlignment="Center" />
                </StackPanel>
            </ComboBoxItem>
        </ComboBox>
    </Grid>
</UserControl>
